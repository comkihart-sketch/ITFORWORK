# 🤖 คู่มือการตั้งค่า Android สำหรับ ShiftFlow

คู่มือนี้สำหรับผู้ใช้งานฝั่ง Android เพื่อสร้างระบบ **ปลุกอัตโนมัติเฉพาะวันที่มีเวร** และ **หน้าต่าง Widget** โดยใช้ API มาตรฐานที่เราเตรียมไว้ครับ

---

## ⏰ ส่วนที่ 1: การตั้งนาฬิกาปลุกอัจฉริยะ (ด้วยแอป MacroDroid)
MacroDroid เป็นแอปสร้างระบบอัตโนมัติที่ใช้งานง่ายที่สุดบน Android เราจะใช้แอปนี้เช็กตารางเวรล่วงหน้าทุกเช้า และสั่งเปิดเสียงปลุกเฉพาะวันที่มีเวรครับ

### ขั้นตอนการตั้งค่า:
1. ดาวน์โหลดแอป **MacroDroid** (ฟรี) จาก Google Play Store
2. เปิดแอปแล้วเลือก **เพิ่มมาโคร (Add Macro)**
3. ตั้งชื่อมาโครด้านบนสุด (เช่น `ปลุกเวรเช้า`) แล้วตั้งค่า 3 ส่วนดังนี้:

**🔴 ส่วนที่ 1: ทริกเกอร์ (Triggers) - ตัวจุดชนวน**
- กดปุ่ม `+` ตรงแถบสีแดง
- เลือกหมวด **วันที่และเวลา (Date/Time)** -> **ทริกเกอร์เวลาปกติ (Regular Time)**
- ตั้งเวลาที่ต้องการเช็กข้อมูลล่วงหน้า เช่น **06:00** (แนะนำให้ตั้งก่อนเวลาตื่นจริงครึ่งชั่วโมง)
- เลือกวันจันทร์-อาทิตย์ แล้วกด ตกลง

**🔵 ส่วนที่ 2: การกระทำ (Actions) - สิ่งที่ต้องทำ**
ให้กดปุ่ม `+` ตรงแถบสีน้ำเงิน เพื่อเพิ่ม 3 คำสั่งตามลำดับนี้:

* **คำสั่งที่ 1 (ดึงข้อมูล API):**
  - เลือกหมวด **แอปพลิเคชัน (Applications)** -> **HTTP Request**
  - เลือกประเภทเป็น **GET**
  - ใส่ URL ด้านล่างนี้ (อย่าลืมเปลี่ยนรหัสพนักงาน):
    `https://itforworkart.vercel.app/api/shortcut/check-shift?username=0157&period=morning`
  - ติ๊กถูกที่ช่อง **"บันทึกข้อมูลตอบกลับเป็นตัวแปร"** (Save body to string variable)
  - กดปุ่ม + เพื่อสร้างตัวแปรใหม่ ตั้งชื่อว่า `ShiftData` แล้วกดตกลง

* **คำสั่งที่ 2 (สร้างกล่องเงื่อนไข 'ถ้า'):**
  - กด `+` เลือกหมวด **เงื่อนไข/ทิศทาง (Conditions/Loops)** -> **บล็อก If (If clause)**
  - กดปุ่ม `+` เพื่อเพิ่มเงื่อนไข -> เลือก **ตัวแปร Macrodroid** -> เลือก `ShiftData`
  - เลือกเงื่อนไข **มีคำว่า (Contains)** แล้วพิมพ์คำว่า **`YES`** (พิมพ์ใหญ่)

* **คำสั่งที่ 3 (ตั้งเสียงปลุก):**
  - กด `+` เลือกหมวด **วันที่และเวลา** -> **ตั้งนาฬิกาปลุก (Set Alarm)**
  - กรอกเวลาที่จะให้ปลุกจริงๆ (เช่น 06:30) และใส่ข้อความ "ตื่นไปเข้าเวรเช้า!"
  - ⚠️ **สำคัญ:** แตะค้างที่คำสั่งนี้ แล้วลากมันเข้าไปใส่ **ไว้ด้านในบล็อก If** (ให้มันอยู่ตรงกลางระหว่าง If กับ End If)

**🟢 ส่วนที่ 3: เงื่อนไข (Constraints)**
- (ไม่ต้องตั้งค่า ปล่อยว่างไว้)

4. กดปุ่มเซฟ (เครื่องหมายถูก มุมขวาล่าง) เป็นอันเสร็จสิ้นระบบปลุกเวรเช้า!
*(สำหรับเวรบ่าย สามารถสร้างมาโครใหม่โดยทำซ้ำขั้นตอนเดิม เปลี่ยนเวลาและเปลี่ยนลิงก์ URL เป็น `period=afternoon`)*

---

## 🌟 ส่วนที่ 2: การสร้าง Widget ข้อความบนหน้าจอ (ด้วย KWGT)
Android สามารถดึงข้อความจาก API มาแสดงบนหน้าจอโฮมได้ผ่านแอปตกแต่ง Widget ชื่อดังอย่าง KWGT

### ขั้นตอนการตั้งค่า:
1. ดาวน์โหลดแอป **KWGT Kustom Widget Maker** จาก Play Store
2. ไปที่หน้าจอโฮมของมือถือ กดค้างที่พื้นที่ว่าง -> เลือกเมนู Widget -> เลือกลาก Widget ของ KWGT (ขนาด 2x1 หรือตามชอบ) มาวาง
3. แตะที่ Widget บนหน้าจอเพื่อเข้าสู่หน้าตั้งค่าแอป
4. กดสร้างใหม่ (Create) หรือเลือกแบบสำเร็จรูป จากนั้นให้กดปุ่ม `+` มุมขวาบน เพื่อเพิ่มตัวอักษร **(Text)**
5. แตะที่ Text ที่เพิ่งสร้าง แล้วหาช่องที่เขียนว่า **Text (สูตรข้อความ)**
6. ลบข้อความเก่าทิ้ง แล้วพิมพ์สูตร (Formula) นี้ลงไป:
   ```text
   $wg("https://itforworkart.vercel.app/api/shortcut/check-shift?username=0157&period=morning", txt)$
   ```
   *(เปลี่ยนเลข 0157 เป็นรหัสของคุณ)*
7. ข้อความจะแสดงผลออกมาเป็น `YES|M05 (เช้า)` หรือ `NO`
8. สามารถปรับแต่งขนาดตัวอักษร (Size) ฟอนต์ หรือสี (Paint) ได้ตามใจชอบ แล้วกดปุ่ม **เซฟ (รูปแผ่นดิสก์มุมขวาบน)** เป็นอันเสร็จ!

*(หากต้องการซ่อนคำว่า NO ให้ดูสวยงาม สามารถปรับสูตร KWGT เป็นขั้นสูงขึ้นได้ เช่น `$if(wg(...)="NO", "วันนี้พักผ่อน", wg(...))$`)*
